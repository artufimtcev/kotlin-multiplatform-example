<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="io.kotlin.everywhere">

	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
	
		<activity
			android:label="@string/repositories_activity_title"
			android:name=".activity.RepositoriesActivity"
			android:theme="@style/RepositoriesActivityTheme"/>
	
		<activity
			android:label="@string/welcome_activity_title"
			android:name=".activity.WelcomeActivity"
			android:theme="@style/WelcomeActivityTheme">
		
			<intent-filter>
			
				<category android:name="android.intent.category.LAUNCHER"/>
			
				<action android:name="android.intent.action.MAIN"/>
			</intent-filter>
		</activity>
	</application>
</manifest>